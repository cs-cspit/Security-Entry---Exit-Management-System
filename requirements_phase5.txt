# Phase 5: Face Recognition Integration - Requirements
# Install with: pip install -r requirements_phase5.txt

# Face Recognition Library (InsightFace)
insightface>=0.7.3

# ONNX Runtime (required by InsightFace)
onnxruntime>=1.16.0
# For GPU acceleration (optional):
# onnxruntime-gpu>=1.16.0

# Additional dependencies for InsightFace
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
albumentations>=1.3.1

# Face alignment and preprocessing
Pillow>=10.0.0

# Existing dependencies (from main requirements.txt)
torch>=2.0.0
torchvision>=0.15.0
torchreid>=1.4.0
ultralytics>=8.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Database (if not already installed)
# No additional DB requirements - using existing SQLite

# Note: InsightFace will automatically download face detection
# and recognition models on first run (~100MB download)
