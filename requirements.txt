# Intelligence-Led Entry & Exit Management System — Python Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: YOLO26 (ultralytics ≥ 8.3) ships ByteTrack + BoT-SORT tracking and
# a built-in Kalman filter. The following libraries are therefore NOT needed:
#   norfair, boxmot, filterpy, deep-sort-realtime
# See requirements_phase6_7.txt for Phase 6/7 specific additions.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Computer Vision & Deep Learning ──────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
scipy>=1.11.0

# ── Detection + Tracking + Pose (YOLO26) ─────────────────────────────────────
# ByteTrack and BoT-SORT are bundled inside ultralytics — no extra tracker lib.
ultralytics>=8.3.0

# ── Person Re-Identification (OSNet) ─────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
torchreid>=1.4.0
gdown>=4.7.1

# ── Face Recognition (Phase 5) ───────────────────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.16.0          # required by InsightFace

# ── Advanced Feature Extraction ──────────────────────────────────────────────
scikit-learn>=1.3.0
scikit-image>=0.21.0

# ── Color Analysis ───────────────────────────────────────────────────────────
webcolors>=1.13
colormath>=3.0.0

# ── Config & Notifications (Phase 7) ─────────────────────────────────────────
pyyaml>=6.0.1                # alert_rules.yaml + system_config.yaml
requests>=2.31.0             # Telegram HTTP notifications

# ── REST / WebSocket API Bridge (Phase 7) ────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-dotenv>=1.0.0         # load TELEGRAM_BOT_TOKEN from .env file

# ── Optional enhancements ─────────────────────────────────────────────────────
# faiss-cpu>=1.7.4           # fast vector search for large registries
# tensorboard>=2.10.0        # training visualisation (not needed at runtime)
# mtcnn>=0.1.1               # alternative face alignment (InsightFace preferred)

# ── Superseded — do NOT install ───────────────────────────────────────────────
# deep-sort-realtime         # replaced by ultralytics built-in ByteTrack
# norfair                    # replaced by ultralytics built-in ByteTrack
# boxmot                     # replaced by ultralytics built-in ByteTrack
# filterpy                   # Kalman filter built into ultralytics tracker
# python-telegram-bot        # replaced by plain requests HTTP call
# deepface                   # replaced by InsightFace (faster, more accurate)
